services:
  - type: web
    name: staff-presence-backend
    env: node
    region: singapore  # Closest to India
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
